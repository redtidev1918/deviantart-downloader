# DeviantArt Downloader - 环境变量示例
# 本工具不会自动读取 .env；请把需要的值设置到终端或系统环境变量中。

# ============================================
# 认证配置 Authentication
# ============================================

# 官方 API（OAuth）登录用的 Public client id。
# 在 deviantart.com/developers 注册 Public 应用获取；也可用
# `devart-dl login oauth --client-id <ID>` 直接传入。
# DEVIANTART_CLIENT_ID=12345

# DeviantArt Cookies（Cookie 降级方案，从浏览器获取）
# DEVIANTART_COOKIES=auth=xxx; auth_secure=xxx; userinfo=xxx

# ============================================
# 代理配置 Proxy Settings
# ============================================

# HTTP/HTTPS 代理（命令行也可用 --proxy 指定）
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# ALL_PROXY=http://127.0.0.1:7890

# ============================================
# 语言配置 Language
# ============================================

# 界面语言 (zh_CN=中文, en_US=English)
# DEVART_LANG=zh_CN

# ============================================
# 安全提示 Security Notes
# ============================================
# ⚠️ 重要：
# 1. 不要将包含真实凭据的 .env 文件上传到 Git
# 2. client_id 不是秘密，但不要分享 Cookie 或 OAuth 令牌
# 3. Cookie 过期后需重新登录（推荐改用 OAuth，自动续期）
# 4. 使用代理时注意代理服务器的安全性
